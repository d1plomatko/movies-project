import css from './NotFound.module.css';

const NotFound = () => {

    return (
        <div className={`${css.not_found} main`}>
            <h1>NotFound</h1>
        </div>
    )

}

export {NotFound}